# Environment Variables Example
# Copy this file to .env.development or .env.production and fill in your values

# Server
NODE_ENV=development
PORT=8000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/artaround_dev

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# OpenAI/Claude API (for translations and AI features)
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_claude_api_key

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# File Upload
MAX_FILE_SIZE=10485760

# Wikidata API
WIKIDATA_API_URL=https://www.wikidata.org/w/api.php

# Startup Seed (useful when you cannot run npm run seed on server)
# If true, seed runs when server starts.
SEED_ON_START=false
# If true (recommended), seed runs only when DB is empty.
SEED_ONLY_IF_EMPTY=true