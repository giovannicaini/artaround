# Navigator Environment Configuration

# API base URL - defaults to /api if not set (for same-origin deployment)
# For development, point to your local server
VITE_API_URL=http://localhost:3000/api

# For production, use the production API URL
# VITE_API_URL=https://api.artaround.example.com/api
